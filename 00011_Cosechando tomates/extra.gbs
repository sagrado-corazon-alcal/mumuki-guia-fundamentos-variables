procedure MoverN(cantidad, direccion) {
  repeat(cantidad) {
    Mover(direccion)
  }
}

procedure SacarN(cantidad, color) {
  repeat(cantidad) {
    Sacar(color)
  }
}

procedure PonerN(cantidad, color) {
  repeat(cantidad) {
    Poner(color)
  }
}